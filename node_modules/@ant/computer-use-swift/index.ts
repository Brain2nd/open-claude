// ComputerUseAPI is a native Swift module on macOS. On other platforms this is a stub.
// Exported as a value (not type) because swiftLoader.ts uses `typeof ComputerUseAPI`.
export const ComputerUseAPI: any = {}
