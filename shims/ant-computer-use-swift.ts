/**
 * Shim: @ant/computer-use-swift
 *
 * The internal package provides macOS-native Swift bindings for screenshots
 * (SCContentFilter), app listing (NSWorkspace), and TCC permission checks.
 * The open-source computer-use-mcp handles these via @nut-tree-fork/nut-js
 * and screencapture CLI, so this shim is a no-op stub.
 */
export const ComputerUseAPI = undefined
